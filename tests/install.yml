---
- hosts: localhost
  connection: local
  roles:
    - lae.travis-lxc
  vars:
    test_profiles:
      - profile: debian-bookworm
      - profile: ubuntu-jammy
      - profile: centos-9
    test_host_suffixes:
      - git
